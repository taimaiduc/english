{% macro create(category, lesson, visibilityDivClass) %}
    <div class="row {{ visibilityDivClass|default('') }}">
        <div class="col-xs-12">
            <ul class="breadcrumb">
                <li class="active"><b>Lesson {{ lesson.position }} - {{ lesson.name }}</b></li>
                {% if lesson.position > 1 %}
                    <li><a class="js-previous-lesson-link" href="{{ path('lessons_show', {'categorySlug': category.slug, 'position': lesson.position - 1}) }}">Previous</a></li>
                {% endif %}
                {% if lesson.position < category.totalLessons %}
                    <li><a class="js-next-lesson-link" href="{{ path('lessons_show', {'categorySlug': category.slug, 'position': lesson.position + 1}) }}">Next</a></li>
                {% endif %}
            </ul>
        </div>
    </div>
{% endmacro %}
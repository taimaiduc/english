{% extends 'AppBundle::layout.html.twig' %}

{% block content %}
<div class="row">
    <div class="col-xs-6">
        <h3>{{ 'form.register'|trans() }}</h3>
        {{ form(form) }}
        <p>
            <a href="{{ path('app.security.login') }}">{{ 'registration.had_account_already'|trans }}</a>
        </p>
    </div>
</div>
{% endblock %}